config {
  type: "view",
  schema : "dataform",
  name : "mv_drink",
  materialized : true
}


SELECT DISTINCT drink_id,
                drink_name,
                drink_type

                FROM ${ref("drinks")}